
#include <gb/gb.h>
#include <gb/cgb.h>
#include <stdio.h>

/*
UINT8 mult; 
struct GameCharacterSmall enem[4];

void setupheart(){
    if(enem[k].initialized==1){
			scroll_sprite(obstacles[k].startspriteid,-speed,0);
			scroll_sprite(obstacles[k].startspriteid+1,-speed,0);

mult = 0;
            enem[mult].x = 135;
            enem[mult].y = 20;
            enem[mult].width = 8;
            enem[mult].height = 8;
    

    //heart sprite tiles
    set_sprite_tile(14, 14);
    enem[mult].spriteidssmall[0] = 14;

    movegamecharactersmall(&enem[mult], enem[mult].x, enem[mult].y);

mult = 1;

    enem[mult].x = 143;
    enem[mult].y = 20;
    enem[mult].width = 8;
    enem[mult].height = 8;
    
    //heart sprite tiles
    set_sprite_tile(14+mult, 14+mult);
    enem[mult].spriteidssmall[0] = 14+mult;

    movegamecharactersmall(&enem[mult], enem[mult].x, enem[mult].y);

    }
    */